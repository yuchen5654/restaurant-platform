# Code Audit — 2026-07-07

Independent review of the Insights Engine (Steps 11–13). Two audits were merged: a primary review and an independent second pass.

---

## Findings

| ID | Area | Severity | Finding |
|----|------|----------|---------|
| F1 | SalesSummary write path | High | `deplete_batch` did not write to `sales_summaries`; `get_covers_insight` therefore always returned null `avg_check`. Additionally, `labor_service` used `SalesSummary` for DOW bucketing — no rows → zero revenue everywhere. |
| F2 | Daypart / DOW timezone | High | `get_sales_patterns` bucketed by UTC weekday and UTC hour. A Friday 11pm sale in New York landed in Saturday's bucket; dinner-hour sales appeared as late-night. |
| F3 | Channel-aware depletion | Medium | `deplete_from_sale` applied all recipe lines unconditionally regardless of `RecipeLine.channel`. Delivery-only packaging was consumed for dine-in sales. |
| F4 | Tab deep links | Low | `InsightsPage` tab state was local (`useState` only). Sharing a URL or reloading always landed on the default tab, not the intended one. |
| F5 | Query invalidation after sales | Medium | `QuickSalesEntry` did not invalidate `food-cost`, `profitability`, `insights`, or `alerts` query caches on success. Dashboard showed stale data until the next automatic refetch. |
| F6 | Settings flush race | Medium | `get_or_create_settings` could raise `IntegrityError` on concurrent first requests (unique constraint on `restaurant_id`). The session was left in an error state. |
| F7 | Action service silent failures | Medium | All six `except Exception: pass` blocks in `get_daily_actions` silently swallowed errors. A broken sub-service caused the entire action list to return incomplete without any log entry. |
| F8 | Missing hot-table indexes | Medium | `sale_adjustments` and `labor_entries` had no index on `(restaurant_id, business_date)`. Full-table scans on every insights request. |
| F9 | Free-form string validation | Low | `channel` on `SaleItem`/`ChannelFeeCreate` and `adjustment_type` on `AdjustmentCreate` were unvalidated strings. Typos (`'doordash'`, `'Comp'`) silently produced orphan data. |
| F10 | Verdict string duplication | Low | `action_service` duplicated the verbose verdict string as a bare literal. Renaming the verdict in `price_experiment_service` would silently break the action filter. |
| F11 | Misc cleanups (5 sub-items) | Low | (a) `get_contribution_margins` missing tenant guard; (b) `get_price_experiments` silently skipped deleted items instead of emitting a placeholder; (c) `actionsData` query had no `staleTime`, refetching on every tab focus; (d) `SECRET_KEY` dev placeholder not warned on startup; (e) redundant null guard before `< -15` numeric compare. |
| F12 | Documentation | — | DECISIONS.md missing entries for upsert strategy, timezone bucketing, recipe channel semantics, and Literal validation. Audit adjudication section required. |
| COR-01 | `_window_metrics` date comparison | Medium | Compared `TIMESTAMPTZ` values against `DATE` column `business_date`; PostgreSQL implicit cast caused incorrect windowing. Fixed to compare `start.date()` / `end.date()` against `business_date`. |
| N+1-01 | N+1 in `get_price_experiments` | Medium | Each event issued a separate `db.get(MenuItem, ...)` and two sub-queries. **Explicitly deferred** — not a correctness issue; address with a JOIN rewrite when query counts are measured in production. |
| N+1-02 | N+1 in `get_contribution_margins` | Low | Per-item `db.get(Ingredient, ...)` calls inside a loop. **Explicitly deferred** — same reason as N+1-01. |

---

## Adjudication (2026-07-07)

Commit: `Audit fixes F1-F12: SalesSummary write path, local-time dayparts, channel-aware depletion, tab deep links, invalidation, settings flush, action logging, indexes, validation, cleanups + audit adjudication`

### Fixed findings

| ID | Status | Notes |
|----|--------|-------|
| F1 | **FIXED** | `_upsert_sales_summary()` added to `depletion_service`; called from `deplete_batch` with `covers` param. `labor_service` DOW bucketing now uses `SalesByItem` directly (no `SalesSummary` dependency). `QuickSalesEntry` passes `covers` in payload. See DECISIONS.md 2026-07-07 (SalesSummary upsert). |
| F2 | **FIXED** | `_get_tz()` + `_localize()` helpers added to `insights_service` and `labor_service`. `get_sales_patterns` and `get_prime_cost` convert `business_date` to restaurant-local timezone before `.weekday()` / `.hour` extraction. See DECISIONS.md 2026-07-07 (DOW and daypart timezone). |
| F3 | **FIXED** | `deplete_from_sale` skips recipe lines where `channel IS NOT NULL AND channel != sale_channel`. See DECISIONS.md 2026-07-07 (RecipeLine.channel semantics). |
| F4 | **FIXED** | `InsightsPage` uses `useSearchParams` from react-router-dom; tab selection writes `?tab=<id>` to the URL and reads it on mount. |
| F5 | **FIXED** | `QuickSalesEntry.onSuccess` invalidates `['food-cost']`, `['profitability']`, `['insights']`, `['alerts']` via `useQueryClient`. |
| F6 | **FIXED** | `get_or_create_settings` wraps INSERT in try/except `IntegrityError`; on conflict rolls back and re-SELECTs. Only `db.flush()` — caller owns the transaction. |
| F7 | **FIXED** | All six `except Exception: pass` changed to `logger.exception(...)` with distinct labels per check. |
| F8 | **FIXED** | Migration `a1b2c3d4e5f6` adds `ix_sale_adjustments_rid_date` and `ix_labor_entries_rid_date`. Applied. |
| F9 | **FIXED** | `SaleItem.channel`, `ChannelFeeCreate.channel`, `AdjustmentCreate.adjustment_type` use `Pydantic Literal`. See DECISIONS.md 2026-07-07 (Literal validation). |
| F10 | **FIXED** | Five verdict constants (`VERDICT_MAINTAINED`, `VERDICT_VOLUME_DROP`, `VERDICT_DECLINED`, `VERDICT_INSUFFICIENT`, `VERDICT_ITEM_DELETED`) defined in `price_experiment_service`. `action_service` imports and uses `VERDICT_VOLUME_DROP`. |
| F11a | **FIXED** | `get_contribution_margins` adds `if not item or item.restaurant_id != rid: continue`. |
| F11b | **FIXED** | `get_price_experiments` checks `if item is None or not item.is_active` and emits placeholder row with `verdict=VERDICT_ITEM_DELETED`. Note: `MenuPriceEvent.menu_item_id` FK prevents hard delete while the event exists; the trigger in practice is soft-delete (`is_active=False`). |
| F11c | **FIXED** | `actionsData` query has `staleTime: 15 * 60 * 1000`. |
| F11d | **FIXED** | `app/main.py` logs a WARNING on startup if `SECRET_KEY` is the dev placeholder or shorter than 32 chars. |
| F11e | **FIXED** | Redundant `units_delta_pct is not None` guard removed; `elif units_delta_pct < -15:` is the direct check. |
| F12 | **FIXED** | This adjudication section + four DECISIONS.md entries (SalesSummary upsert, timezone bucketing, RecipeLine.channel, Literal validation). |
| COR-01 | **FIXED** | `_window_metrics` now compares `start.date()` / `end.date()` against `business_date`. |

### Verified false positives

None identified. All findings were confirmed valid.

### Deferred (not fixed in this session)

| ID | Reason |
|----|--------|
| N+1-01 | Correctness is unaffected; rewrite requires JOIN restructure across the experiment loop. Defer until production query counts are measured. |
| N+1-02 | Same rationale as N+1-01. |
